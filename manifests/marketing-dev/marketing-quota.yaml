apiVersion: v1
kind: ResourceQuota
metadata:
  name: marketing-quota
  namespace: marketing-dev
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "4"
    limits.memory: 8Gi
    pods: "10" # Ograniczenie liczby podów